# len1, len2 = len(str1), len(str2)

#     def isDivisor(1):
        # if len1 % 1 or len2 % 1:
        #     return False
        # f1, f2 =len1 // 1, len2 // 2
        # return str1[:1] * f1 == str1 and str1[:1] *f2 == str2

        # for l in range(min(len1, len2), 0, -1):
        #     if isDivisor(1):
        #         return str1[:1]
        # return ''